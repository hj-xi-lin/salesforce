<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ON_PaymentDue__c</fullName>
    <description>Defines the payment due period of the invoice.</description>
    <externalId>false</externalId>
    <formula>IF(
    Bulk_Invoicing__c,
    IF(
        NOT(ISBLANK(Account.Parent.Parent.Payment_Time__c)),
        Account.Parent.Parent.Payment_Time__c,
        Account.Parent.Payment_Time__c
    ),
    CASE(
        Payment_Time__c, 
        null, 
        14, 
        Payment_Time__c
    )
)</formula>
    <inlineHelpText>Defines the payment due period of the invoice.</inlineHelpText>
    <label>JustOn: Payment Due</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
