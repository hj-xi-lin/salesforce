<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ON_ServiceDate__c</fullName>
    <description>The record will be invoiced when the date is null or within the service period of the invoice run.</description>
    <formula>IF(
    Bulk_Invoicing__c = FALSE,
    TODAY(),  
    DATE(YEAR(TODAY()), MONTH(TODAY()), 1)
)</formula>
    <inlineHelpText>The record will be invoiced when the date is null or within the service period of the invoice run.</inlineHelpText>
    <label>JustOn: Service Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
