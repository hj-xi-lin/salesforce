<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Mandatory_fields_for_Closed_Lost</fullName>
    <active>true</active>
    <description>RO-1569, RO-1558</description>
    <errorConditionFormula>AND(
ISCHANGED( StageName ),
ISPICKVAL( StageName , &quot;Closed Lost&quot;),
   
OR(
    ISPICKVAL( plLossReason__c, &quot;&quot; ),
    AND(
        NOT($Profile.Name = &apos;System Administrator&apos;),
        NOT(
            OR(
                ISPICKVAL(plLossReason__c, &quot;Admin Only: Clean-Up Sales Funnel&quot;),
                ISPICKVAL(plLossReason__c, &quot;Already won in SSSU Process&quot;)
            )
        ),
        ISBLANK( Loss_Reason_Detail__c )
    )
)
)</errorConditionFormula>
    <errorMessage>Please lost the opportunity by clicking on the &quot;Closed Lost&quot; button located on the top right corner</errorMessage>
</ValidationRule>
